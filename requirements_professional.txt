# Professional Income Generation Setup
# Lightweight version for Android/Termux compatibility

# Core requirements (lightweight)
-r requirements.txt

# Professional image processing (no heavy ML dependencies)
# Real-SR moved to requirements_realsr.txt due to PyTorch/size issues

# Lightweight professional tools
psutil          # For memory monitoring
tqdm            # For progress bars in batch processing

# API-based upscaling alternatives (lightweight)
# waifu2x-ncnn-vulkan  # Uncomment for local GPU upscaling (if available)

# Note: For Real-SR AI upscaling, use requirements_realsr.txt
# But be aware it requires ~2-3GB of PyTorch dependencies